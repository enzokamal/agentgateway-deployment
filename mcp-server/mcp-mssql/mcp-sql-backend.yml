apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayBackend
metadata:
  name: mcp-mssql-backend
spec:
  mcp:
    targets:
    - name: mcp-mssql-target
      static:
        host: mcp-mssql-service.agentgateway-system.svc.cluster.local
        port: 8000
        protocol: StreamableHTTP
